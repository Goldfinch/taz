<?php
{{ namespace }}
use {{ __use_pagetype }};
use SilverStripe\Forms\FieldList;

class {{ class }}Extension extends {{ __pagetype }}
{
    private static $db = [];

    public function updateCMSFields(FieldList $fields)
    {
        $fields->initFielder($this->owner);

        $fielder = $fields->getFielder();

        // ..
    }
}
